module IBS
  module InternetBsApi
    module Domain
      module UrlForward
        def forward_domain(forward_source, forward_target)
          options = {"Source" => forward_source, "Destination" => forward_target}
          connection.post("Domain/UrlForward/Add", options)
        end

      end
    end
  end
end
